# 本地 Codex 订阅对战

## 当前本机试玩入口（2026-09-16）

已完成一局内存自然终局及 4 次真实前端短测：续轮缓存、批量盖牌、前端订阅用量与导出账本一致。前端短测 4/4 决定接受，3/3 续轮命中缓存，总输入 179,253（缓存 116,992、非缓存 62,261）、输出 1,913；在 T1 按计划手动结束，不称为第二局自然终局。此前运行中的 API 仍是独立 exec，短测重启后才启用同线程与换线程；不能只根据磁盘代码或内存实验判断网页的实际模式。

当前机器的可复用启动器在仓库外 `../ai_battle/AI对战基础设施/start-local-codex-api.sh`。它只启动/重启本地 API，不重建测试环境、不运行迁移或导入、不修改 `.env`，不调用模型；Node 路径及本机依赖检查沿用已核验的恢复脚本。本机文件不随本仓库提交，其他机器应按下方环境配置单独接入。

从本仓库目录执行：

```bash
# 只检查配置；不重启或请求模型
bash ../ai_battle/AI对战基础设施/start-local-codex-api.sh --check
# 重启现有 loveca-test:api，会中断内存对局并清空观察材料，先结束对局并导出
bash ../ai_battle/AI对战基础设施/start-local-codex-api.sh --restart
```

`--restart` 会把该 tmux 窗口的启动命令固定为同一脚本的 `--run`；以后只重启此窗口可复用配置。完整删除并重建 tmux 测试环境后，仍需再次执行上面的 `--restart`，不能以全套测试环境重建代替 API 重启。不存在该窗口时明确报错；`--run` 可在已有数据库、编译输出和前端环境中前台运行 API，不会创建依赖服务。

该启动器显式设置以下进程配置（覆盖旧短测上限；不是仓库或生产默认值）：

```dotenv
AI_BATTLE_LOCAL_CODEX=1
API_HOST=127.0.0.1
AI_BATTLE_CODEX_REASONING=medium
AI_BATTLE_CODEX_SESSION_REUSE=1
AI_BATTLE_CODEX_THREAD_ROTATION=1
AI_BATTLE_CODEX_MAX_CALLS=60
AI_BATTLE_CODEX_MAX_INPUT_TOKENS=9000000
AI_BATTLE_CODEX_MAX_UNCACHED_INPUT_TOKENS=1500000
AI_BATTLE_CODEX_MAX_OUTPUT_TOKENS=50000
```

每局预算沿用已完成自然局的 60 次/900 万总输入/150 万非缓存，输出另设 5 万，避免短测 1 万限制阻断完整局；这不是实际消耗承诺或代理新增调用授权。最后一次响应可能超过 token 阈值，新局重新计数，不能用它限制整日或整个 ChatGPT 账户额度。本机 API 仍只允许一局未结束 Codex 对局，原有未知用量停止、认证、超时和历史保护继续有效。

刷新 `http://localhost:5173/`，选择 **Luna + 中等（medium）**、构筑与先后手即可创建试玩。前端首次加载仍默认 low，并显式提交该选择，服务端 medium 默认不会覆盖它；每次新打开页面请检查强度。Qwen 的开关和上游配置不变。同线程首轮及换线程首轮允许零缓存，续轮是否命中以实际用量为准，缓存比例不能直接换算套餐消耗。

本局观察的“模型配置”应显示 `EPHEMERAL_APP_SERVER_EXPERIMENT`、`FIRST_TURN_OF_EACH_GENERATION`、`threadRotation: true`，预算应为上述完整试玩值。为保持公平，进行对局时只看本局用量，结束后再查看 AI 私有观察。调试材料有保留上限；结束后及时导出，长局只在最后导出可能缺少早期决定。当前已知两项展示问题尚未修复：批量 LIVE 的命令摘要可能显示“等待或未执行”，以及结束后的观察仍提示真人牌桌继续运行；以权威执行结果与实际会话状态核对。

以下日期章节保留实验历史，历史余额、测试服状态及“下一步”不能覆盖本节当前结论。新的前端验收产物位于仓库外 `frontend-acceptance-20260916/`。

## 2026-09-16 修复版首次完成自然局：T6、44次、4次换线程

在上一批牌顶失效修复上完成一局自然终局：44次Luna medium回答全部合法执行，4次实际水位自动切换均成功，39/39续轮缓存命中。上下文分别212,324→49,738、228,792→69,598、231,337→82,974、223,289→87,166；第27次在连续卡效中间正确续接。总输入5,728,233、缓存4,643,584（81.06%）、非缓存1,084,649、输出18,807，44次用量全报告。预算60次/900万输入/150万非缓存均未触及，余16次，未追加实验。T1～T6各一次LIVE_SET选择3/3/2/3/2/3张，原链完成。

真实修复版牌顶审计覆盖3次已知顶牌、7窗口，未发现过期牌顶残留；当前窗口、事件移交并集、材料SHA-256、账本与状态独立审计通过。最终有界导出淘汰72条决定，逐次快照并集保留1～157无编号缺口，不把最后一份导出称为完整历史。本轮无新运行源码修改，沿用同一修复版5文件62项及服务端类型检查；新增真实验证及源码哈希/diff核验。证据在仓库外`rotation-natural-fixedtop-20260916/`。

同线程/换线程及修复版完整局已获得一局实证，下一步转本地前端验收与使用说明，不再重复短缓存实验。T2低费反复登场回收及模型理由资源不一致仍属策略问题，固定Bot终局不代表策略合格；缓存比例不代表套餐节省率。默认实验开关、测试服环境、生产和子模块未动；本批修复及验证记录纳入本次 amend，不推送。

## 2026-09-16 自然局两次自动换线程通过，预算停止；修复过期牌顶记忆

新自然局30次Luna medium全部合法执行，27/27续轮命中；两次实际水位切换上下文222,015→52,820、217,223→64,678，新线程首轮零缓存、续轮恢复。输入3,546,593，缓存2,942,208（82.96%）、非缓存604,385，输出13,822；30次用量均已报告。T5达到非缓存60万停止阈值，未自然终局、未重试/重开；剩10次不能绕过本轮token停止条件。T1～T5各一次LIVE_SET选择3/2/0/1/1张。历史移交121/193条均来自本席已送达事件，缺口明确；材料与计费独立审计通过。

本席复盘发现第25次抽牌后knownDeckTop仍指向已入手的分数6「ハナムスビ」：公开日志没有回合抽牌移牌事件，旧上下文仅靠事件失效。现以本席可见主牌库张数与公开移动增减对照，存在未解释变化时清除牌顶记忆，同批置顶后未报告抽牌也清除；不读牌序或新增规则。真实SAMPLE离线回放30→31→30复现旧版错误并确认修复，0额外模型调用；5文件62项相关测试与服务端类型检查通过。自然局运行于修复前，修复后尚未真实模型全局验收。另记录成员HEART/声援收益理由错误，未改专家策略。

下一步优先完成修复版自然终局验收，另定预算；不再重复短缓存实验，也不默认启用实验开关。缓存比例不等于套餐额度节省，30次无混淆不证明永久消除。原始证据及逐回合记录在仓库外`rotation-natural-window-20260916/`。运行源码只改牌顶记忆失效，测试服、环境、子模块未动；尚未提交或推送。

## 2026-09-16 当前窗口提醒后的短回放通过

原第5/6窗口在新线程连续回放2次：EFFECT/CARDS返回c1，随后MAIN/ACTION返回当前a6，均经原候选校验通过、未执行。两轮末尾摘要与当前任务一致，wire schema保持相同。总输入70,460、缓存28,416、非缓存42,044、输出1,425；续轮输入41,521中缓存28,416，余额11次。

短样本仅含2窗口，第二轮上下文42,603，较此前错误时的75,807更短；不能证明提醒单独消除了旧窗口混淆，也未验证真实水位自动切换或自然终局。下一步建议集中做有界自然局，停止重复同类短回放；开局前明确足够预算，现有11次余额不能保证终局。本批运行源码未变，沿用上一批99项及服务端类型结果；真实调用和独立载荷/候选/schema/用量审计通过。证据在仓库外`window-transition-20260916/`。

## 2026-09-16 真实规模回放在旧窗口回答处停止

后续6次 Luna medium只读冻结输入回放，第6窗口T2 MAIN/ACTION却返回了前一EFFECT/CARDS窗口的c1选牌任务，原候选校验拒绝，未执行、重试或重开。发送的当前purpose/space正确；这是当前窗口回答失败，未达到自动切换水位，也不能直接推算自然实战错误率。已知输入287,524、缓存185,344、非缓存102,180、输出2,909，6次全部报告，余额13次；最后上下文75,807，未达206,720水位。

Codex请求末尾新增当前turn/phase/purpose/selectionKind摘要，使当前任务位于长卡文之后。固定schema、原规则链与错误拒绝不变，不自动改写模型输出或重试。新增CARDS→ACTION回归，相关2文件99项、服务端类型检查通过；真实改善效果未验证。下一步先做短连续窗口验证，暂不继续昂贵的水位累积。证据在仓库外`rotation-scale-20260916/`；本批无真实对局或测试服变更。

## 2026-09-16 实际换线程及续轮通过小样本验证

后续3次 Luna medium合成调用全部成功：旧线程完成后实际调用`createSuccessor`，新线程首轮重发固定资料、虚构已知历史与当前观察，续轮只追加当前观察。上下文13,765→8,313→8,426；新线程续轮总输入8,378，其中缓存7,936（94.72%）、非缓存442。三轮当前引用、当前分数、旧公开弃牌和对手隐藏信息UNKNOWN均正确，第3轮输入未重复提供弃牌事实。

本批总输入30,300、缓存7,936、非缓存22,364、输出204，余额19次。只验证低水位提前切换的session接口与缓存恢复，尚未验证真实规模下client自动水位切换或完整自然局，也未查明此前间歇性失败根因。下一步应转向真实规模自动切换验收，不再重复小样本缓存实验。无运行源码、默认配置或测试服变更；沿用上一批98项定向测试及服务端类型结果，本批另做真实诊断、独立账本/事实审计和diff检查。证据在仓库外`rotation-successor-20260916/`。

## 2026-09-16 后续合成诊断：未换线程也会失败

获准最多2次，实际首轮3.67秒即失败，尚未调用换线程接口；安全诊断为`TURN_RUNNING / TURN_FAILED`。用量未知，未发送第二次；本批累计授权余额23次。该结果证明失败不以换线程为必要条件，不能证明此前自然局与本次同因，也未验证新线程缓存恢复。

补充CLI失败轮的`codexErrorInfo`白名单与有效HTTP状态码，不记录错误正文、additionalDetails或stderr；未知类别标为UNKNOWN，不增加自动重试。7项新增假CLI测试，相关2文件98项、服务端类型及diff检查通过。用户另行确认后，第二次同份prompt/schema调用成功：输入8,246、缓存0、输出93，当前引用及事实正确；两次合计1次用量未知、1次成功，余额22次。尚未调用换线程接口。相同请求呈间歇性失败/成功，根因未明；诊断改动不等于修复。下一步仍以小规模切换验证为先，不新开自然局。账本在仓库外`rotation-diagnostic-20260916/`。[官方错误字段说明](https://learn.chatgpt.com/docs/app-server#errors)。

## 2026-09-16 自然局：首次换线程后失败，尚未验收

用户批准最多40次 Luna medium，总输入400万、非缓存60万停止阈值。仅本地内存自然局，数据库访问拒绝、固定测试策略对手、既有冻结卡面，未重启测试服。实际尝试16次：前15次全部合法执行，14个续轮全部命中；已知输入1,585,100，其中缓存1,363,456（86.02%）、非缓存221,644，输出6,021。T1/T2/T3分别一次选择3/1/3张盖牌并经原链设置确认。

T4第15次后上下文213,160超过206,720水位。第16窗口成功构造新线程请求，但调用3.75秒后失败，未返回响应或用量，按约定立即停止、未自然终局、未重试或重开。计费attempts=16、reported=15、unreported=1；以上token仅为已知值，第16次不算免费，次数余额24也不代表可以绕过本轮失败停止条件继续。TRANSPORT_ERROR显示未取消、新线程尚无上下文报告；现有证据不足以区分上游失败与本地协议处理失败，更不能归因于缓存。

离线审计确认新请求的115条公开事件全部来自本席已收到的输入，92个未见序号明确保留缺口；没有旧完整局面或另一席私有trace。最终15个模型决定状态ACCEPTED、失败窗口STOPPED；导出无淘汰、省略、采集失败或不完整材料。独立固定策略离线预检曾在T13第43窗口达到128KiB事件上限停止（0次模型调用），属于实验容量限制。

为解决首个定位缺口，TRANSPORT_ERROR新增固定枚举的会话阶段/失败类型，以及安全整数RPC错误码；不导出错误正文、stderr、凭据、任意上游字段，也不增加重试。新增5项假CLI回归覆盖RPC错误、turn失败、工具/RPC拒绝与进程退出，2个文件91项通过，服务端类型检查通过。该改动不能恢复本次已丢弃的错误原因。下一步先用有界合成材料复现换线程并读取新诊断，再决定修复；暂不默认启用换线程或交付作者使用。证据、账本、逐回合记录在仓库外`rotation-natural-20260916/`。

## 低频换线程实验开关

新增 `AI_BATTLE_CODEX_THREAD_ROTATION=1`，默认关闭，要求同时启用 `AI_BATTLE_CODEX_SESSION_REUSE=1`。达到 CLI 报告窗口80%的既有水位后，在下一次请求发送前关闭旧线程；新线程发送固定资料、此前本席收到的公开事件和当前作者输入。没有原生自动压缩、模型摘要或旧完整局面复制；不重开对局、不重置计费/可选预算、不重试上一决定。账户约束跨线程保留，登录换号会在新模型请求前停止。

历史只来自当前client已经收到的input.history.events，去重保留原事件与序号缺口；不从权威状态或另一席trace补信息。副本上限128KiB，缺失历史、序号回退、冲突或超限即停止。历史身份不证明现在隐藏位置，当前资源和仍有效的已知牌顶以作者context/观察为准；不迁移任意旧私密检视快照。REQUEST增加generation，换线程记录THREAD_ROTATION。

此前真实窗口15～17只读回放发现最近12事件会遗漏曾送达的旧公开选牌入手事实。保留已送达事件后，修正审计枚举的16、17窗口候选和事实检查通过，17窗口输入67,924中缓存48,896，上下文68,274；原连续线程在16窗口后已220,921。一次真实大小续轮命中不保证稳定缓存或长局策略正确。

本批共3次Luna medium（包含15窗口一次审计格式失败），输入168,160、缓存48,896、非缓存119,264、输出1,321；授权累计30/40，剩10次。没有执行任何回放建议，未完成新的自然局。定向5文件112项与服务端类型检查、diff检查通过；新开关尚未在测试服启用或做完整局验收。证据在仓库外`rotation-real-window-20260915/`。

## 可选实验预算

未设置环境变量时，不强加调用次数或累计 token 预算；已撤掉此前未经充分讨论加入的默认5次、固定token限制。每项预算独立可选，只能在本地 API 环境显式设置正安全整数，未设置的项不参与限制：

```dotenv
# 以下只是短测示例，不是默认配置；按实验目标选择需要的项。
AI_BATTLE_CODEX_MAX_CALLS=5
AI_BATTLE_CODEX_MAX_INPUT_TOKENS=500000
AI_BATTLE_CODEX_MAX_UNCACHED_INPUT_TOKENS=150000
AI_BATTLE_CODEX_MAX_OUTPUT_TOKENS=10000
```

已设置的预算新建时冻结，前端用量浮层显示各项已用与阈值（未设置项明确标注）。发送前按已有记录检查，达到阈值则停止后续请求；未发送请求不计数，非法回答的已发生调用照常计数。token依赖完成后的报告，最后一次可能超阈值；计数为provider决策次数，不保证CLI内部HTTP次数或套餐百分比。用量未知仍停止且不执行该次返回动作；超时、取消、原上下文水位保护仍保留。

同一本地 API 服务仅允许一局未结束 Codex 对局，包含创建预留，原 API 容量不变。限制不跨进程或项目，新开对局重新计数。预算只属于内存会话和已有模型配置材料，没有新增数据库迁移或改变持久计费结构。没有修改 `.env`、重启测试服或让运行中的旧进程自动获得新行为。

## 新线程切换合成验证（2026-09-15）

旧线程完成窗口后关闭，新线程只输入固定资料、确定的合法历史与当前观察；后续两轮继续同一新线程。本批4次 Luna medium，六项事实与当前候选检查全部通过，上下文29,319降到7,141 token。输入50,770（缓存6,912、非缓存43,858），输出296，合并此前授权已用27/40次、余13次。

新线程三轮输入7,041/7,199/7,307，缓存0/6,912/0：观察到了恢复命中，也观察到随后又为零，不能保证稳定缓存。此为约3万token上的提前切换，旧快照是合成填充；历史由脚本明确提供，没有模型摘要、真实对局或自动压缩。没有验证真实长局切换点及作者有界历史的完备性，也不能推算套餐节省率。

本批定向验证：会话与客户端2文件79项通过（最终客户端60项复跑通过），前端预算组件4项通过；共享、服务端、前端类型检查和暂存/未暂存diff检查通过。未做浏览器验收。

当时尚未接入换线程，后续真实窗口验证及默认关闭的实验开关见本文首节。完整证据在仓库外 `thread-rotation-20260915/`；工具结果配合原生压缩的路线暂缓。

## 当前实现：同线程与作者输入整合

按用户确认，显式 `AI_BATTLE_CODEX_SESSION_REUSE=1` 恢复同一局、同一席的临时线程续接。首轮发送固定资料和当前观察，后续只追加作者统一输入构造的当前观察及有界合法历史；固定资料仍在线程上下文中，不是免费或已删除。保留作者的候选压缩和最终集合批量盖牌。默认独立 exec、Luna low 和环境文件不变；本轮有界验证单独选择 Luna medium。

容量保护改用 CLI 报告的上一轮输入加输出 token 数，达到报告窗口的 80% 时停止下一次请求；没有窗口报告时暂用 150,000 token 水位，没有有效用量时不继续。请求字节数只用于 512 KiB 运输大小限制，不再与 token 相加，也不把历史累计字节作为 token 限制。这是基于上一轮的预警线，不能保证下一轮一定不超过上游容量；上游错误仍停止且无重试。REQUEST 使用 `APPEND_TO_EPHEMERAL_THREAD`，RESPONSE 提供实际上下文与停止水位，保护停止记录 `CONTEXT_GUARD`。

本轮暂保留禁用自动压缩的实验设置，先验证同线程与作者更新这个组合。用户批准最多 40 次 Luna medium、总输入约 600 万（含缓存）、非缓存目标 80 万；到预算、异常或上下文保护停止，不自动重开。若触及水位，再单独评估压缩前后事实保留与缓存恢复，不能声称已经测试自动压缩。

首次新局实际调用 3 次，均合法执行；总输入 97,938，其中缓存 55,808、非缓存 42,130，输出 806。两个续轮均命中，CLI 报告窗口 258,400、保护线 206,720，最后上下文为 40,618。局面在 T1 被测试 Bot 的阶段结束命令中断：试打脚本使用了不带服务层 3 秒门禁的原始玩家投影，规则链正确拒绝。不是模型、缓存或容量失败；首局已清理，未自动重开。证据在仓库外 `same-thread-author-20260915/`。

后续脚本改为读取 Bot 自己的 `getMatchSnapshot`，遵守 `TIME_GATE.availableAfterMs` 后再查询和提交；没有读取另一席隐藏信息或修改游戏规则。修复脚本的离线验证使用固定策略代替模型，21 个模拟决策、T5 自然终局，真实模型调用为 0。用户随后明确确认新开一局，沿用剩余最多 37 次与扣除首局后的 token 预算。源码回归 6 个文件共 122 项通过，服务端类型检查通过；默认配置、测试服与生产未改，尚未提交或推送。

修复后新局实际调用 16 次，16 次均为合法 MODEL 选择并权威执行成功，15 个续轮全部命中缓存。累计输入 1,784,606，其中缓存 1,554,688（87.12%）、非缓存 229,918，输出 6,871。T1/T2/T3 的 LIVE_SET 分别一次返回 2/1/2 张最终集合，由原规则链设置并确认，没有逐张模型调用。52 个决定材料无淘汰、省略或采集失败；公开事件仍是有界采样，不是完整回放。

T4 最后上下文为 220,921，超过报告窗口 258,400 的 80% 水位 206,720，因此第 17 个窗口发送前停止，未自然终局，未发生自动压缩或上游容量报错。第 15 次完成后只有 206,535，故当时允许了第 16 次；水位不预测下一次增长。两次真实局合计已使用 19/40 次调用，输入 1,882,544、非缓存 272,048，剩余 21 次。本次证明同线程缓存和作者批量盖牌可共同工作，不能证明完整长局、策略强度或套餐节省比例。其后的原生压缩诊断见下节。完整证据在仓库外 `same-thread-author-20260915b/验证结论.md` 和 `analysis.json`。

## 原生压缩诊断（2026-09-15）

新增显式 `CodexBattleSession.compact(signal)` 仅供诊断，正常对战不自动调用；同线程、账号与取消隔离继续生效，遇到非预期工具或未完成压缩即停止。真实合成验证使用 3 次 Luna medium 决策和 1 次原生压缩，当前引用、分数、公开事实与隐藏信息边界检查全部通过。但旧用户正文仍在，不能据此证明压缩摘要保留事实。上下文由 13,255 变为压缩后估计 14,064，后续实际 13,554，未减小。压缩后首轮零缓存，再下一轮 13,624 输入中命中 13,056。

已报告输入 40,319、缓存 13,056、非缓存 27,263、输出 167。原生远程压缩未报告可核算用量，必须计作一次调用且用量未知，不能把重置后的零值当免费。合并前两局使用 23/40 次、余 17 次；合计已知输入 1,922,863、非缓存 299,311，均排除未知压缩用量。

真实 CLI 连接回环假上游的多窗口对照发现：原生压缩后，作为用户消息发送的三份旧完整观察仍原样保留；因此不能只打开自动压缩来解决当前输入增长。独立动态工具实验中，旧观察放在只读 `read_current_window` 的结果里时，压缩后旧正文被移除且缓存键保留。此为 CLI 请求重建的离线证据，假上游不证明摘要质量或真实缓存；工具方式每窗口可能增加一次模型往返，尚未接入对战。

当时提出的受限工具方案暂缓，后续优先验证低频新线程切换，见本文前述新线程验证。正常对战、环境和测试服未改。定向 2 个文件 63 项通过，服务端类型检查通过。完整账本与离线抓包在仓库外 `native-compact-20260915/`。

## 历史实验：2026-09-15 作者更新整合与独立线程

本地 Codex 工作树已整合作者 AI 分支 `ea66bb73`，待提交；作者分支引用保持不变。保留 Qwen / GLM / DeepSeek、API 思考开关和最终多选盖牌＋确认。Codex 复用作者统一的输入构造，前端保留 low / medium，默认仍为 Luna low。印刷声援基线的缺口字段改为 `printedBaselineShortfallLiveRefs`，避免把尚未计算补心卡效的基线误称为必败。

显式 `AI_BATTLE_CODEX_SESSION_REUSE=1` 现在只复用隔离进程，每窗口新建临时线程，完整发送固定资料、当前观察和作者已有的有界合法历史，完成后释放线程。REQUEST 标记 `contextMode=FRESH_EPHEMERAL_THREAD`、`includesStaticKnowledge=true`。默认独立 exec 和默认关闭的实验开关未改。取消、异常不重试模型或回退 API；清理失败保留已收到的结果和用量，然后关闭客户端。

**新模式修复了旧局面累积，但缓存尚未解决。** 本轮获准最多 4 次 Luna medium、总输入目标约 10 万，实际使用 2 次：输入各 4,785，共 9,570；缓存均为 0；输出共 71。两次正确返回当前 a1 / b1。另 2 次未使用。下文旧连续线程的缓存命中证据不适用于新模式，也不能推算套餐节省。

离线假上游确认新线程不携带旧窗口，但缓存标识也发生变化；该差异不能证明是零命中的唯一原因。空白线程分叉被 CLI 拒绝，临时线程 rollback 要求持久化历史；均未接入源码。下一步应先评估隔离临时目录中的线程历史和接口可维护性，再验证同标识裁剪方案。本批未修改环境、重启测试服或开始新的真实对局。

证据位于仓库外 `ai_battle/AI对战基础设施/integration-20260915/`。复制离线脚本时，旧目录 `cache-diagnostic-20260914/appserver-wire.json` 被新独立线程的假上游抓包覆盖，该抓包已另存新目录，不再作为旧追加模式证据；原模型账本、请求和响应未改。以下按日期保留历史实验记录。

验证：12 个定向测试文件先通过 205 项；随后新增三项客户端/会话回归，受影响的两个文件复跑 57 项通过，最终共 208 项。覆盖三张盖牌一次返回后的原规则链执行、连续 12 个大窗口不累积历史容量、清理失败保留已报告用量、API/Codex 创建参数、超时与停止。共享、服务端、前端类型检查及暂存 diff 检查通过。没有新的真实模型批量盖牌或自然完整局验收；本批没有提交或推送。

本地实验分支 `feat/local-codex-ai-battle` 基于作者 PR #56 的 `dcf639ac`。保留 Qwen 上游、平台配置与权威规则链，增加本机 Codex provider。默认使用精简对战指令的独立 `exec`；临时 app-server 会话复用仅作显式实验；2026-09-15 Luna medium 合成与真实内存规则续轮均已命中缓存；真实样本在 T2 触发历史容量保护，尚不能用于整局。默认模型为 **Luna (`gpt-5.6-luna`)**，推理强度为 **low**；不继承开发窗口的模型或思考强度。此入口仅供本机自用，不得通过代理或隧道发布成公开模型服务。

## 启用条件与早期恢复记录

本节中的 9 月 13 日启动器是历史恢复方式；当前机器优先使用文首可复用启动器，旧脚本不会启用同线程/自动换线程。

开关是服务端配置，前端提供模型选择器；选择 Codex 后可为新对局设置轻度（low）或中等（medium），默认轻度。启用 Codex 仍是服务端开关。

2026-09-13 已恢复当前 `loveca-test:api`，Codex 开关已开启，后续新局默认 Luna low，直接刷新前端即可。之前提供的裸 `tmux respawn-pane -e ...` 指令依赖新 shell 的 PATH；现场复现新 shell 找不到 `node`，API 窗口退出，前端因而断连。不要对旧启动命令继续使用该指令。

本机已建立仅启动 API 的恢复脚本（仓库外，无凭据内嵌）：

```text
../ai_battle/AI对战基础设施/restart-local-codex-api-20260913.sh
```

当时 API 窗口使用该脚本，明确设置已核验 Node 的 PATH、读取并验证现有本地测试配置，再启动 API。当前请使用文首的明确启动命令；只执行不带命令的 tmux respawn 会沿用该窗口上次的配置，可能仍是旧配置或短测配置。脚本不执行数据库重建、迁移、种子导入或卡库同步命令。它临时将 API 对象存储指向本机，避免接触 `.env` 中的远端存储；本机 MinIO 未运行，所以上传功能暂不可用，未修改 `.env` 或 Vite 图片代理配置。

重启后访问 `http://localhost:5173`，在测试账号的管理中心进入“对局与赛季 → AI 对战调试”，刷新页面后模型应默认显示 `gpt-5.6-luna · ChatGPT 订阅（本地）`。无需进入“AI 上游配置”填写 Qwen Key。选好双方构筑和 AI 手册，选择真人先后手后创建对局；“观察材料”中查看模型选择、校验与实际执行结果，“本局”用量上悬停或点击可展开 token 统计。

若以后使用当前版本启动脚本重新建立测试环境，下面的 `.env` 方式也可用，因为当前脚本已在启动命令中加上 `--env-file-if-exists=.env`。不要把磁盘上新版脚本的行为等同于仍在运行的旧进程。

开关默认关闭。现有测试 API 使用的本地 `.env` 或启动环境需明确包含：

```dotenv
AI_BATTLE_LOCAL_CODEX=1
API_HOST=127.0.0.1
AI_BATTLE_CODEX_REASONING=low
# macOS 默认路径；只有实际安装位置不同时才需要覆盖
AI_BATTLE_CODEX_PATH=/Applications/ChatGPT.app/Contents/Resources/codex
```

同时要求 `NODE_ENV=development`、`DATABASE_URL` 指向回环主机，以及 `FRONTEND_URL` 为本机 HTTP 地址，例如 `http://localhost:5173`。当前磁盘上的测试启动脚本会显式传入这些原测试环境参数，并让 API 读取 `.env` 中的额外设置。修改后需重新加载 API 进程；不要为了开关调用会重建数据库、同步卡库的全套测试环境脚本。本批没有修改 `.env` 或创建真实对局；当前 API 启动参数已通过上述恢复脚本明确启用。

启用后，API 实际绑定指定回环地址；所有 `/api` 请求还检查原始 socket 对端、Host、Origin 和代理来源，覆盖可能间接唤醒 AI 的共享对局命令入口。前端 Origin 只允许配置的本机端口（localhost/127.0.0.1/::1 别名），或者 API 自身 origin。Vite API 代理开启 `xfwd`，追加真实对端；非回环来源、跨站来源或不明确的 Forwarded 头被拒绝。

这些条件是明确的部署约束，不是自动证明“机器上没有生产数据”。不得把本机生产转发端口伪装成测试库，也不得用会丢失真实来源信息的代理暴露入口。当前已核实的测试库是 localhost:5432/loveca，0039/0040 已迁移；不需要平台 Qwen 上游配置就能创建 Codex 对局。

前端从受权限保护的 `/models` 获取可用模型。关闭开关只提供 Qwen，伪造 Codex 创建请求也会被服务拒绝；开启后默认选 Luna，可手动选择其他已登记的 Codex 模型或 Qwen。现有管理员权限和对局所有权检查继续生效。

创建请求可选 `reasoningEffort: low | medium`，仅对 Codex 有效；不提供时沿用服务端默认。前端明确提交选定值，服务端校验并冻结到本局 provider、会话及模型配置观察，不改写全局环境变量。Qwen 不显示此选项，也拒绝携带该字段的创建请求。降低强度不减少每次发送的上下文。

## 调用、观察与用量

- CLI 版本暂限已验证的 `0.153.4`、`0.154.0-alpha.6.2`；后者于 2026-09-14 重新验证严格参数、临时目录外文件读取拒绝及真实结构化返回后加入。其他版本仍拒绝；升级后应先重新核验隔离选项，不自动降级。每次调用检查 ChatGPT 登录，子进程环境采用白名单，不传 API Key、数据库凭据、桌面线程路由等变量。`forced_login_method=chatgpt` 阻止切换 API 认证。
- 默认每请求使用新临时目录、stdin 与结构化输出，`model_instructions_file` 替换 CLI 默认编码指令；忽略用户配置和规则，不加载项目 AGENTS 或主机技能。禁用 shell、MCP 配置、插件、应用、浏览器、计算机操作、代码执行宿主、记忆等入口。权限配置只开放最小运行路径与当前临时工作目录，命令网络关闭。禁止工具访问的提示词仅作为补充。
- 固定知识与紧凑玩家观察复用作者输入。CLI wire schema 固定描述 ACTION/CARDS 输出形状，当前引用只放在输入中，原始完整 `responseSchema` 仍留在请求正文；分组、数量、去重、跳过等合法性由原响应解析与权威规则链裁决。CLI 返回 JSON 不表示动作已执行。
- 单次 Codex 窗口上限 90 秒；取消会终止进程组，500 ms 后仍未退出则强制终止。超时或 CLI/认证失败停止 AI，不自动重试模型、不转 Qwen/API。已返回的非法候选仍由作者原有失败/兜底策略处理。Qwen 原超时和重试策略保留。
- CLI 的启动 `item.error` 是诊断记录，不当作工具执行；`turn.failed`、缺失完成事件、工具执行记录或超限输出拒绝。日志不导出 CLI stderr。真实烟测已出现并覆盖“代码执行宿主已禁用”诊断。
- Codex 的 `ai_billing.prices`、`pricingDate` 与投影 `estimatedCny` 为 null，前端显示 ChatGPT 订阅及 token 用量，不显示 ¥0。输入与缓存输入分开累计；缺失或矛盾 usage 保持未知。当前未确立缓存写入的计量口径，出现非零 `cache_write_input_tokens` 时记为未知，不静默套用 Qwen 口径。调用次数指 provider 决策请求：默认一次 exec，实验模式一次 app-server turn；不宣称等于内部 HTTP 请求数或套餐剩余额度。
- Qwen 既有记录格式及价格保持原样。新增 Codex JSON 记录需要本分支的读取代码；旧作者分支不了解订阅计费格式。不要为切分支删除历史记录，也不要把 Codex 用量伪装成 Qwen。

## 验证与尚未完成项

定向测试覆盖本地门禁、API 登录拒绝、订阅显示与累计、取消后进程退出、非法/迟到响应、停止策略及原 Qwen 回归；HTTP 测试采用内存数据与临时端口，不操作业务测试库。

2026-09-13 两次无真实对局数据的 CLI 烟测分别使用 Luna low 和最终默认 Astra low；Astra 返回当前唯一合法引用，耗时约 9.6 秒，报告输入 9,342、输出 104 token。另用同一实际沙箱权限配置验证目录内测试文件可读、目录外测试文件被操作系统拒绝。产物保存在仓库外 `ai_battle/AI对战基础设施/`。

用户已进行前端真实试打并导出会话；导出时尚未结束，累计 62 次调用。第 6 回合盖三张与确认使用 4 次调用、254,986 输入 token，证明逐步决策有重复请求开销，不代表完成自然终局或策略验收。分析保存在仓库外 ai_battle/AI对战基础设施/会话调用开销分析-1e660c2d-20260913.md。

Luna 默认值与逐局强度选择验证：3 个测试文件共 77 项通过，共享/服务端/前端类型检查通过；Vite 页面模块和公开 health/ready 均返回 200。未为此调用真实模型或执行浏览器点击验收。

官方参考：[认证](https://learn.chatgpt.com/docs/auth#openai-authentication)、[非交互调用](https://learn.chatgpt.com/docs/non-interactive-mode)、[权限配置](https://learn.chatgpt.com/docs/permissions)。GPT 推理在云端执行，本机运行的是 CLI。

## 2026-09-14 最终接入状态与缓存证据

**截至 2026-09-14 尚未命中；9 月 15 日 medium 对照已命中，详见末节。临时会话复用仍不设为默认。** 默认路径维持每次独立 `exec --ephemeral`，每次完整发送固定知识和当前观察，但已用短对战指令替换 CLI 内置编码长指令。真实 CLI 连接本机假上游的对照中，该段正文由 17,766 降为 316 UTF-8 bytes；这是实际请求正文验证，不是整局 token / 套餐额度节省比例。默认输出 schema 也已固定，当前 token 合法性仍由原解析和权威链裁决。

经用户两次授权，共完成 7 次 Luna low 合成调用，总输入 45,856、缓存 0、输出 232：原独立 CLI 三次输入 10,206 / 10,200 / 10,200；短临时会话两轮 2,780 / 2,840；长临时会话两轮 4,785 / 4,845。不同实验材料长度不同，不能直接把这些数字相除当作节省率。四次新会话调用都正确选择当前窗口引用，续轮未误用旧引用；未创建真实对局。

离线确认独立 CLI 的缓存标识及内部 arg0 路径会变化；同一临时 app-server 线程的请求标识稳定且第二轮完整保留首轮前缀。然而短、长真实连续窗口均未命中，证明这些客户端改动并不充分。剩余问题可能涉及实际渲染前缀、CLI 或 ChatGPT 订阅服务端的缓存条件；目前没有证据判定具体是哪一项，API 公共文档也不能直接证明此订阅链路必定命中。不能把“首次资料只传一次”写成“后续历史免费”，服务端仍会处理线程历史。

### 临时会话实验开关

仅需要继续验证此方案时，在现有本地环境显式设置 `AI_BATTLE_CODEX_SESSION_REUSE=1` 并重新加载 API，新建的 Codex 对局才采用实验模式；省略或 `0` 保持独立 exec。本批没有修改 `.env` 或替用户开启实验。前端“本局模型配置”观察中，`EPHEMERAL_EXEC` 与 `EPHEMERAL_APP_SERVER_EXPERIMENT` 可区分两种路径。保持原回环、开发环境和 ChatGPT 登录门禁。

实验实现使用每局每席独立线程、一次在途 turn；静态知识首轮发送，后续追加完整当前脱敏观察。REQUEST 记录 `sessionTurn`、`includesStaticKnowledge` 和 `contextMode`；历史选择不表示已执行，原规则链决定实际结果。固定 schema 允许 ACTION/CARDS，变化的候选、数量与分组约束留在当前输入。

app-server 使用独立空白 CODEX_HOME 和工作目录，不复制用户配置、规则或 auth.json。通过官方 `chatgptAuthTokens` 接口把现有 CLI 登录的 access token 与 account ID 传入子进程内存，凭据不进入模型输入、日志或 trace，不读取其他对局资料。该认证接口为实验接口；现有登录必须可从 CLI auth.json 读取，不支持只有 keyring 的登录。账号变化会停止；如上游要求刷新，只接受桌面/CLI 已更新的同账号 token，至多提供一次，绝不自行改写原登录或切换 API Key。实际调用已验证此路径可用。

拒绝工具执行、审批 RPC 和异常输出；终局、显式结束、停止、超时、取消与初始化失败均关闭进程组并清理临时目录。服务正常退出或收到 SIGTERM/SIGINT 时也清理进程；被 SIGKILL 或机器断电无法保证清理，因此仍不把敏感历史写入持久 rollout。每轮用量取 `tokenUsage.last`，缺失仍为未知，不重复累加线程 total。

当前实验不自动压缩历史：发送前以累计输入/输出正文 1 MiB、上一轮已知输入 token 加本轮 UTF-8 字节的保守上界 150,000、以及报告上下文窗口的 75% 为限，超限停止且不自动新建会话恢复。这是调试边界，尚未完成长局容量验收。

9 月 14 日待办为取得相同长固定资料的缓存证据；9 月 15 日 medium 对照已完成该项，下一步转为有界真实窗口验收，再决定是否默认启用会话复用。Qwen 和盖牌动作批处理未改。

原始账本、虚构请求、离线请求对比及复跑脚本在仓库外 `ai_battle/AI对战基础设施/cache-diagnostic-20260914/`。官方参考：[app-server 与外部 ChatGPT 认证](https://learn.chatgpt.com/docs/app-server)、[缓存规则](https://developers.openai.com/api/docs/guides/prompt-caching)、[配置](https://learn.chatgpt.com/docs/config-file/config-reference)。

最终验证：4 个定向测试文件分批共 95 项通过（Codex 客户端/门禁 43、会话/清理 11、运行器 22、HTTP 路由 19）；共享、服务端和前端类型检查通过，最后初始化清理调整后再次通过服务端类型检查及 54 项 Codex 测试。git diff --check 通过。本机 /api/health、/api/ready 均 HTTP 200。未运行全量构建或自然完整对局。

## 原生续接追加对照

2026-09-14 用户追加批准两次 Luna low 原生 CLI 对照，实际使用 21,724 新输入、缓存 0。采用带相同安全隔离的 `exec → exec resume <具体ID>`，没有 Loveca provider、输出 schema、指令替换、外部 token 登录或 ephemeral；离线请求证明完整前缀及缓存键稳定，但仍非完全默认 TUI，不能排除共同隔离设置。`exec resume` 本次报告累计 usage，已由仅属于本次合成会话的 `last_token_usage` 核对：首轮 10,610、续轮 11,114；不能把累计 21,724 再加首轮。当前实战默认独立 exec 与实验 app-server last 用量不受此计数差异影响。

此前用户真实会话导出的模型实际为 Astra，并记录过 13,824 缓存输入；截至 9 月 14 日的 9 次受控诊断全部为 Luna low，CLI 也比此前更新。当时待办为取得正常 Luna 用量基准，再区分模型、版本与隔离配置差异；不把零命中泛化为 Codex 订阅不支持缓存。本批保持运行源码和默认配置。详细证据在仓库外 `cache-diagnostic-20260914/原生续接对照结论.md`。

## 2026-09-15 medium 单因素配置对照：首次命中

先只读历史会话的模型、强度、CLI 版本及用量元数据，找到 4 个旧版 CLI 0.153.4 的 Luna medium 派生会话有缓存。它们带继承历史、输入较长，不作为当前配置的直接对照，也未读取其对话或工具正文。

经用户批准新增最多 2 次 Luna medium 合成调用、输入目标不超过 12,000 token，使用当前 CLI 0.154.0-alpha.6.2 和既有 `CodexBattleSession`。逐字复用先前长前缀 low 实验的两轮 prompt/schema，只把配置的 reasoningEffort 改为 medium；线程模式、认证和文件/工具隔离保持不变。离线核对两轮 prompt/schema 均完全相同。

| 实验 | 首轮总输入 / 缓存 | 续轮总输入 / 缓存 | 两轮输出 |
| --- | ---: | ---: | ---: |
| 先前 Luna low | 4,785 / 0 | 4,845 / 0 | 58 |
| 本次 Luna medium | 4,784 / 0 | 4,866 / 3,840 | 95 |

本次总输入 9,650，其中缓存 3,840、非缓存 5,810；续轮缓存占比 78.9%，非缓存仅 1,026。两轮分别正确返回 a1 / b1，耗时约 4.6 / 2.2 秒，均正常完成并关闭临时会话。没有新增真实对局或额外模型调用。实际账本、输入和响应位于仓库外 `cache-diagnostic-20260914/medium-ledger-20260915.json`、`medium-1-20260915.json`、`medium-2-20260915.json`。

这证明现有会话复用、短指令、隔离与订阅认证的组合能够命中缓存；medium 是当前有证据支持的下一项测试配置。实验跨日且使用不同线程，无法控制上游路由/缓存状态，因此不能断言 low 普遍不支持缓存，也没有证明 medium 是唯一原因或所有隔离设置均无影响。缓存 token 比例不等于套餐额度节省比例；输出增加和长局历史增长仍须实际计量。

下一步建议在用户准备好后，以本地 `AI_BATTLE_CODEX_SESSION_REUSE=1` 加逐局 Luna medium 做少量真实决策验收，检查配置标记、每轮非缓存/缓存/输出、引用合法性和历史容量，再决定默认值与盖牌批处理。**本批未更改运行源码、环境文件、默认 Luna low 或默认关闭的会话复用开关，未重启测试服、提交或推送。** 本次仅更新证据与文档，未重复运行此前已通过的源码测试。

## 2026-09-15 真实规则窗口：持续命中，但历史增长阻塞

在用户最多 10 次 Luna medium 授权内，新建一次自然随机的内存绿莲镜像对局，AI 先手、固定测试策略对手。复用原 preset loader、Codex 客户端、会话、用量、trace 和 OnlineMatchService 的权威队列，脚本串行控制调用数；未通过 HTTP 或浏览器驱动，也未启用自动 AiBattleDriver。卡面使用仓库既有 hash 锁定的测试事实，规则/手册来自原 assets；不声称等同于测试库发布卡快照。数据库访问显式拒绝，持久化使用内存，未重启或修改测试服。

实际发送 9 次模型调用，9 次均返回合法当前引用、MODEL 来源并被权威链成功执行；覆盖换牌、登场、卡效弃牌/检视选牌、结束主要阶段、连续盖牌及确认、T2 换手。8 个续轮全部命中。总输入 637,605，其中缓存 512,000（80.30%）、非缓存 125,605，输出 2,951。固定知识仅首轮发送，逐轮 usage 与 trace 一致；计费 attempts/reportedAttempts 均 9，pending/unreported 为 0，trace 无淘汰、遗漏或采集失败。此比例不是套餐额度节省率。

**首个真实阻塞为窗口 10 的 T2 MAIN：本地发送前容量保护。** 上一轮输入 119,955 加本轮正文 38,151 bytes 的保守上界为 158,106，超过最多 150,000 的本地保护线，因此没有发送第 10 次模型调用，授权尚余 1 次。不是上游拒绝，也不是自然终局。当前统一“调用失败”提示掩盖具体容量原因，应随后改进。仅提高上限不能解决逐轮追加完整快照造成的历史增长。

下一步应优先收束稳定固定资料、当前完整观察与有界合法历史的输入方式，核实官方线程历史操作语义及缓存保持，再做有界验证；不把整局追加完整快照设为默认，不先采用未经验证的模型摘要。之后再处理盖牌/确认重复请求。本批仅修改文档与仓库外诊断产物，未改运行代码、默认 Luna low 或实验开关；没有重复运行已有源码测试，文档 diff 检查通过。详细账本和同前端导出格式的受控席 trace 在仓库外 `ai_battle/AI对战基础设施/real-window-cache-20260915/`，含私有试打材料，不纳入 Git。
